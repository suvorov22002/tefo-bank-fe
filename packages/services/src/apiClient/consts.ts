export enum HeaderConsts {
  ContentType = 'Content-Type',
  Authorization = 'Authorization',
}

export enum ContentTypeConsts {
  Json = 'application/json',
  Urlencoded = 'application/x-www-form-urlencoded',
  FormData = 'multipart/form-data',
}
